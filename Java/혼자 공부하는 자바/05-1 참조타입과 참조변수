### 자바 타입 ###
- `기본 타입` : byte, short, char, int, long, float, double, boolean 타입 <br>
값 자체가 저장됨 <br>
- `참조 타입` : 배열, 열거, 클래스, 인터페이스 등 (조건식)<br>
객체의 번지가 저장됨, 번지로 객체를 참조한다는 의미에서 참조 타입<br>
- `메모리 사용 영역` : JVM은 메모리영역을 메소드, 힙, 스택 영역으로 구분해서 사용<br>
`메모리영역`에는 정적필드, 상수, 메소드 코드, 생성자 코드가 위치.<br>
`힙영역`에는 객체가 생성됨. <br>
`스택영역`에는 변수가 생성됨. <-- **변수는 참조, 기본타입 모두 스택에 형성됨** <br>
- `null` : 참조 타입변수가 객체를 참조하지 않는다는 뜻으로 null값을 가질 수 있음.<br>
`null`인 참조변수를 통해 있지도 않은 객체의 필드나 메소드 사용 시, `NullPointerException`이 발생함.<br>


### 공부할만한 포인트 ###
- `메모리 사용 영역` 부분이 잘 이해되지 않음.
---
### Ref. ###
혼자 공부하는 자바

