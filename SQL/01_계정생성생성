-- 모든 권한은 system계정에서 부여

-- 새로운 계정 만들기 & 접속 권한 부여
CREATE USER GREEDY IDENTIFIED BY GREEDY;
GRANT RESOURCE,CONNECT TO GREEDY;

-- EMPLOYEE/EMPLOYEE
CREATE USER EMPLOYEE IDENTIFIED BY EMPLOYEE;
GRANT RESOURCE,CONNECT TO EMPLOYEE;

-- 뷰(VIEW)를 생성할 권한 부여 
GRANT CREATE VIEW TO EMPLOYEE(부여할 사용자)

-- 동의어(SYNONYM)를 생성할 권한 부여
GRANT CREATE SYNONYM TO EMPLOYEE;
